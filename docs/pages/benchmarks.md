@page benchmarks Benchmarks

# INCOMPLETE FILE

This page should contain reproducible performance measurements for RuKh.

## Environment

Record CPU, RAM, operating system, kernel, compiler, build configuration, OpenSSL, SQLite, and liburing versions.

## HTTP Benchmarks

Measure representative workloads such as plaintext keep-alive, TLS, static files, streaming responses, compression, and concurrent connections.

Record throughput, latency distribution, concurrency, and error rate.

## Database / ORM Benchmarks

Measure representative lookups, inserts, updates, bulk operations, joins, hydration, prepared-statement reuse, and relation traversal.

## Methodology

Record the benchmark tool, command line, warm-up period, measurement period, number of runs, and result aggregation method.

## Results

Put verified benchmark results here. Do not use estimates.

## Interpretation

Explain what the measurements imply about the current architecture and distinguish measurements from hypotheses.
